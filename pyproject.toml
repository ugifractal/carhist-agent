[project]
name = "carhist-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "langchain>=1.3.18",
    "langchain-core>=1.6.1",
    # "langchain-google-genai>=4.4.0",  # GEMINI — temporarily disabled, switch to Groq (free tier)
    "langchain-groq>=0.3.0",  # GROQ — free tier (openai/gpt-oss-120b)
    "langgraph>=1.2.11",
    "python-dotenv>=1.2.3",
    "uvicorn>=0.52.4",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
